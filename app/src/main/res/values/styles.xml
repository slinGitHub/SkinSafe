<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="ToolbarTitleBold">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/DarkColor1</item>
        <item name="android:textFontWeight">400</item>
        <!-- 100 = sehr dünn (Thin / Hairline)
            400 = normal (Regular)
            700 = fett (Bold)
            900 = extra fett (Black / Heavy) -->
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="AboutPageSkinSafeTitle">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/DarkColor1</item>
        <item name="android:textFontWeight">400</item>
        <!-- 100 = sehr dünn (Thin / Hairline)
            400 = normal (Regular)
            700 = fett (Bold)
            900 = extra fett (Black / Heavy) -->
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="ImageTitle">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/colorSecondaryBlackText</item>
        <item name="android:textFontWeight">400</item>
        <!-- 100 = sehr dünn (Thin / Hairline)
            400 = normal (Regular)
            700 = fett (Bold)
            900 = extra fett (Black / Heavy) -->
        <!--<item name="android:fontFamily">@font/bahnschrift</item>-->
    </style>

    <!-- TextStyle für deine TextViews -->
    <style name="TextViewTextStyle">
        <item name="android:textColor">@color/colorSecondaryBlackText</item>
        <item name="android:textSize">16sp</item>
    </style>


</resources>